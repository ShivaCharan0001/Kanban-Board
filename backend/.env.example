# Server Configuration
PORT=5000
NODE_ENV=development

# Database Connection (MongoDB Atlas connection string or local MongoDB URI)
MONGODB_URI=mongodb+srv://<username>:<password>@cluster0.xxxxx.mongodb.net/kanban?retryWrites=true&w=majority

# JSON Web Token Secret (Choose a strong random string)
JWT_SECRET=your_super_secret_jwt_key_here
